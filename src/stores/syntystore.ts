import { PacksData, Pack, PackOrigin } from "../models";
import { UAS_POLYGON_SERIES_LINK, UAS_SIMPLE_SERIES_LINK } from "../constants";

const onLoad = () => {
  console.log("syntystore on loaded");
}

// listeners
window.addEventListener("load", () => {
  onLoad();
});