import React from "react";
import ReactDOM from "react-dom";

export const Popup = ({ }) => {
  return (
    <>
      <div>Example</div>
    </>
  );
}

ReactDOM.render(
  <React.StrictMode>
    <Popup />
  </React.StrictMode>,
  document.getElementById("root")
);