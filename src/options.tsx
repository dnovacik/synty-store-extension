import React from "react";
import ReactDOM from "react-dom";

export const Options = ({ }) => {
  return (
    <>
      <div>Options Example</div>
    </>
  );
}

ReactDOM.render(
  <React.StrictMode>
    <Options />
  </React.StrictMode>,
  document.getElementById("root")
);